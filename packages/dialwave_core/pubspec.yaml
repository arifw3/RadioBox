name: dialwave_core
description: Shared models for DialWave — used by both the nightly radio_sync script and the Flutter app.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.10.0

dependencies:
  meta: ^1.15.0

dev_dependencies:
  lints: ^5.0.0
  test: ^1.25.0
