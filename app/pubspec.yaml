name: dialwave
description: "A new Flutter project."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+1

environment:
  sdk: ^3.10.4

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter

  # Multi-language support (Turkish default, English so far).
  flutter_localizations:
    sdk: flutter
  intl: any

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8

  # Shared radios.json model — kept in sync with tool/radio_sync so the
  # nightly script and the app never drift on the JSON shape.
  dialwave_core:
    path: ../packages/dialwave_core

  # Background-safe playback: Foreground Service + MediaSession/lock-screen
  # controls (Section 4 of CLAUDE.md).
  audio_service: ^0.18.15
  just_audio: ^0.9.42
  # Audio focus: ducking for navigation prompts, pause/resume around phone
  # calls (Section 4 of CLAUDE.md).
  audio_session: ^0.1.21

  flutter_riverpod: ^2.6.1
  http: ^1.2.0
  shared_preferences: ^2.3.0

  # Sole revenue source: one thin bottom banner, never interstitial/audio
  # ads (Section 9 of CLAUDE.md).
  google_mobile_ads: ^5.2.0

  # Drive Mode auto-trigger via GPS speed (Section 6 of CLAUDE.md).
  geolocator: ^13.0.1

  # Dynamic color palette from the playing station's logo (Section 7 of
  # CLAUDE.md).
  palette_generator: ^0.3.3

  # Radio wake-up alarm (Section 7 of CLAUDE.md).
  flutter_local_notifications: ^18.0.1
  timezone: ^0.9.4
  flutter_timezone: ^3.0.1

  # Social Sync: shared listening rooms + live listener count (Section 8
  # of CLAUDE.md). Firebase Realtime Database on the free Spark plan.
  firebase_core: ^3.8.0
  firebase_database: ^11.1.6
  share_plus: ^10.1.2

  # Crash/error reporting — free on the Spark plan, no server to run.
  firebase_crashlytics: ^4.1.5

  # Brand typeface for the redesigned UI.
  google_fonts: ^6.2.1

  # Voice-first full-screen search — typing a station name while driving
  # is impractical, so search leads with on-device speech recognition and
  # falls back to a keyboard.
  speech_to_text: ^7.0.0

  # Disk-persistent image cache — station logos shouldn't re-download on
  # every cold start.
  cached_network_image: ^3.4.1

  # A meaningful slice of manually-sourced station logos turn out to be
  # SVG (brand sites often only publish a vector logo) — CachedNetworkImage
  # can't decode those at all, see widgets/station_art.dart.
  flutter_svg: ^2.0.10

  # Local persistence: cache-first catalog load, Wi-Fi-only setting,
  # in-app review triggers.
  path_provider: ^2.1.5
  connectivity_plus: ^6.1.0
  in_app_review: ^2.0.10
  url_launcher: ^6.3.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^6.0.0

  # Generates real Android/iOS launcher icons from assets/branding/icon.png.
  flutter_launcher_icons: ^0.14.2

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:
  generate: true

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  assets:
    - assets/branding/

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package

flutter_launcher_icons:
  android: true
  ios: true
  image_path: "assets/branding/icon.png"
  # The mark has transparent corners around a circular badge — Android
  # adaptive icons need an opaque background or launchers render it on
  # whatever wallpaper is behind it. White matches the hand-tuned icon
  # set actually shipped (images/icons/) — keeps a future regeneration
  # from silently reverting to a different background.
  adaptive_icon_background: "#ffffff"
  adaptive_icon_foreground: "assets/branding/icon.png"
